---
description: Validate OpenCode config JSON syntax
---

Validate the OpenCode configuration file at `.opencode/opencode.json` for JSON syntax errors.

Run: `bun .opencode/tools/validate-config.ts .opencode/opencode.json`

If there are any JSON syntax errors, they will be displayed with the line and error details.
