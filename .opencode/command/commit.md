---
description: Commit changes with proper formatting and grouping
agent: git
subtask: true
---

Commit $ARGUMENTS using git workflow best practices.

Analyze changes with `git status` and `git diff`, group logically, and commit with proper message format:
{Subject/Module/System} // {Message}

Add summary and context after first line for complex changes.
